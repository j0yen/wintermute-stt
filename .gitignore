/target
**/*.rs.bk
Cargo.lock.bk
.DS_Store
# subagent isolation worktrees (Claude Code Agent --isolation=worktree
# creates these inside the slice; never part of the slice artifact)
.claude/worktrees/
# whisper.cpp model binaries — large blobs, installed via install.sh --download-model
# See README for model download instructions and licensing.
models/
*.bin
